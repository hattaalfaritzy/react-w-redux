import { Users } from '@/views';

const users = [
    { path: 'users', element: <Users /> },
    // { path: 'users/:id', element: <DetailEnrollment /> },
    // { path: 'users/add', element: <AddEnrollment /> },
    // { path: 'users/edit/:id', element: <EditEnrollment /> },
];

export default users;
